# Claude Code instructions

Read and follow `AGENTS.md` before changing this repository. Its release
discipline is mandatory: every pull request increases the Chief version,
keeps the lockfile synchronized, runs `npm run release:check`, and updates only
the helper documentation whose contract actually changed.
