# Claude Code instructions

Read and follow `AGENTS.md` before changing this repository. Its release
discipline still applies: run `npm run release:check`, keep the lockfile
synchronized, and update only the helper documentation whose contract actually
changed. Version bumps are no longer required on every PR — bump the Chief
version only when you intend to ship a release (see AGENTS.md).
